{% extends 'base.html.twig' %}

{% block title %}Personnage{% endblock %}

{% block titre %}Personnage {{ perso.pseudo }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-2">
            <img src="{{ asset('images/personnages/' ~ perso.pseudo|lower ~ '.png') }}" alt="">
        </div>
        <div class="col">
            <h2>{{ perso.pseudo }}</h2>
            <div>
                {{ (perso.sexe) ? 'Homme' : 'Femme' }}
            </div>
            <h3>Les caractéristiques</h3>
            <ul>
                <li>Force: {{ perso.carac.force }}</li>
                <li>Force: {{ perso.carac.agi }}</li>
                <li>Force: {{ perso.carac.intel }}</li>
            </ul>
        </div>
    </div>
{% endblock %}